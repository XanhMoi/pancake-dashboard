# Dependencies
node_modules/

# Secrets — NEVER commit / deploy these
pos-token.txt
*token*.txt
*.har

# Dữ liệu phân quyền (users.json = hash mật khẩu, config.json = token Pancake)
data/
monitor-state.json

# Logs & temp
server.log
*.log
_chk.js
.env
.env.*

# Editor/Claude
.claude/
